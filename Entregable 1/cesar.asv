function salida = cesar(entrada,desplazamiento)
a=double(entrada)
for i=1:length(a)
    aux=a(i);
    if 96< a(i)<123
    	val=97;
    else
        val=65;
    end
    aux=mod(aux - val + desplazamiento,26);
    aux=a(i)+val;
end
salida=char(a);
end
